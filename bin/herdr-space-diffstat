#!/usr/bin/env python3
"""Entry point for the herdr-space-diffstat plugin."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "lib"))

from hsd.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
